import "react"
export type ListProps = {
    /**
     * What the Label looks like disabled
     */
    disabled?: boolean;
    /**
     * What the string is in the list component
     */
    text: string;
  }