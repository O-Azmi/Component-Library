import "react"
export type TableCellProps = {
    /**
     * What the width of the table cell will be
     */
    width: string;
    /**
     * What the table cell will look like disabled
     */
    disabled?: boolean;
    /**
     * What the height of the table cell
     */
    height:string;
    /**
     * What the background color of the cell will be
     */
    backgroundColor: string;
    /**
     * Choose what side the border will be on
     */
    border: string;
    /**
     * Text inside the table cell
     */
    text:string;
    /**
     * If text should be bolded
    */
    bold: string;
    }

