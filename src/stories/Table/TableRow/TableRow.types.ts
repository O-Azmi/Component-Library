import "react"
export type TableRowProps = {
    /**
     * What the table row will look like disabled
     */
    disabled?: boolean;
    /**
     * Prop that lets you next children
     */
    children?: React.ReactNode;
  }

