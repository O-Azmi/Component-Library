import "react"
export type TableFooterProps = {
    /**
     * What the width of the table footer will be
     */
    width: string;
    /**
     * What the table footer will look like disabled
     */
    disabled?: boolean;
    /**
     * What the height of the table footer
     */
    height:string;
    /**
     * What the background color of the table footer will be
     */
    backgroundColor: string;
    /**
     * Choose what side the border will be on?
     */
    border: string;
    /**
     * Text inside the table footer
     */
    text: string;
  }

